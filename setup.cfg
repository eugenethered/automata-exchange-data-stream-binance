[metadata]
name = automata.data.stream.binance
version = 0.0.1
description = Automata Binance Data Stream
license = UNLICENSE

[options.entry_points]
console_scripts =
    executable-name = binance.__main__

[options]
packages = find:
install_requires =
    automata.data.stream==0.0.1

[options.packages.find]
include = data*
exclude =
    tests
    simulations
